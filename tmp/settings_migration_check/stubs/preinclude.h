// The shift_replay stub set, plus the two declarations read_subsystem_settings
// needs and which its stub eeprom_config.h (which replaces the real header) omits.
#include "../../shift_replay/stubs/preinclude.h"
#include "nvs.h"
extern nvs_handle_t MAP_NVS_HANDLE;
